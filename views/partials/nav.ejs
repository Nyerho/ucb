<nav class="nav">
  <div class="nav-inner">
    <a href="/" class="brand">
      <div class="brand-logo">UCB</div>
      <div class="brand-text">
        <span class="brand-name">United Credit Bank</span>
        <span class="brand-tag">🇦🇺 Australia's Trusted Bank</span>
      </div>
    </a>
    <button class="btn btn-light nav-mobile-toggle d-lg-none" type="button" data-bs-toggle="offcanvas" data-bs-target="#publicNavMenu" aria-controls="publicNavMenu">
      ☰ Menu
    </button>
    <ul class="nav-links d-none d-lg-flex">
      <li><a href="/" class="<%= page === 'home' ? 'active' : '' %>">Home</a></li>
      <li><a href="/services" class="<%= page === 'services' ? 'active' : '' %>">Services</a></li>
      <li><a href="/about" class="<%= page === 'about' ? 'active' : '' %>">About</a></li>
      <li><a href="/contact" class="<%= page === 'contact' ? 'active' : '' %>">Contact</a></li>
    </ul>
    <div class="nav-actions d-none d-lg-flex">
      <% if (typeof user !== 'undefined' && user) { %>
        <% if (user.is_admin === 1) { %>
          <a href="/admin/dashboard" class="btn btn-outline btn-sm">Admin Panel</a>
        <% } else { %>
          <a href="/user/dashboard" class="btn btn-outline btn-sm">Dashboard</a>
        <% } %>
        <a href="/auth/logout" class="btn btn-primary btn-sm">Logout</a>
      <% } else { %>
        <a href="/auth/login" class="btn btn-outline btn-sm">Login</a>
        <a href="/auth/register" class="btn btn-primary btn-sm">Open Account</a>
      <% } %>
    </div>
  </div>

  <div class="offcanvas offcanvas-end" tabindex="-1" id="publicNavMenu" aria-labelledby="publicNavMenuLabel">
    <div class="offcanvas-header">
      <div>
        <div class="fw-bold" id="publicNavMenuLabel">United Credit Bank</div>
        <div class="text-muted small">Responsive menu</div>
      </div>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body">
      <div class="mobile-nav-links">
        <a href="/" class="mobile-nav-link <%= page === 'home' ? 'active' : '' %>">Home</a>
        <a href="/services" class="mobile-nav-link <%= page === 'services' ? 'active' : '' %>">Services</a>
        <a href="/about" class="mobile-nav-link <%= page === 'about' ? 'active' : '' %>">About</a>
        <a href="/contact" class="mobile-nav-link <%= page === 'contact' ? 'active' : '' %>">Contact</a>
      </div>
      <div class="mobile-nav-actions">
        <% if (typeof user !== 'undefined' && user) { %>
          <% if (user.is_admin === 1) { %>
            <a href="/admin/dashboard" class="btn btn-outline btn-block">Admin Panel</a>
          <% } else { %>
            <a href="/user/dashboard" class="btn btn-outline btn-block">Dashboard</a>
          <% } %>
          <a href="/auth/logout" class="btn btn-primary btn-block">Logout</a>
        <% } else { %>
          <a href="/auth/login" class="btn btn-outline btn-block">Login</a>
          <a href="/auth/register" class="btn btn-primary btn-block">Open Account</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
